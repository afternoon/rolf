{external, "loadtime.sh"}.

{frequency, 10}.

{title, "Load Time"}.
{vlabel, "Secs"}.

{metrics, [
    {loadtime, [
        {label, "Load Time"},
        {type, gauge},
        {draw, areastack},
        {min, 0},
        {colour, "#0091FF"}]}]}.
